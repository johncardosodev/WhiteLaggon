@model List<Extra>

<div class="w-100 card border-0 p-4">
    <div class="card-header bg-success bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2">Lista de villas Cardoso Resort</h2>
            </div>
        </div>
    </div>
    <div class="card-body border p-4">
        <div class="row pb-3">
            <div class="col-6 offset-6  ">
                tes
            </div>


            <div class="col-6 offset-6 text-end">
                <a asp-controller="Extras" asp-action="Create" class="btn btn-secondary">
                    <i class="bi bi-plus-circle"></i> Criar novo Extra
                </a>
            </div>
        </div>

        <table class="table table-hover" align="center">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model[0].Nome)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model[0].Descricao)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model[0].Villa.Nome)
                    </th>
                    <th>
                        Opções
                    </th>
                </tr>
            <tbody>
                    @foreach (var item in Model)
                    {
                    <tr>
                        <td>
                                @Html.DisplayFor(modelItem => item.Nome)
                        </td>
                        <td>
                                @Html.DisplayFor(modelItem => item.Descricao)
                        </td>
                        <td>
                                @Html.DisplayFor(modelItem => item.Villa.Nome)
                        </td>


                        <td>
                            <div class="w-75 btn-group" role="group">
                                <a asp-controller="Extras" asp-action="Atualizar" asp-route-extraId="@item.Id" class="btn btn-success mx-2">
                                    <i class="bi bi-pencil"> Modificar</i>
                                </a>
                                <a asp-controller="Extras" asp-action="Apagar" asp-route-extraId="@item.Id" class="btn btn-danger mx-2">
                                    <i class="bi bi-trash"> Apagar</i>
                                </a>
                            </div>
                        </td>
                    </tr>
                    }
            </tbody>
        </table>
    </div>
